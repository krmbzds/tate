require 'spec_helper'

describe Transliterate do
  it 'has a version number' do
    expect(Transliterate::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
